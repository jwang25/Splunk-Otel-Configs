clusterName: "johnw_cluster" #change this
splunkObservability:
  realm: "us0" #change this
  accessToken: "" # change this
  metricsEnabled: true
  tracesEnabled: false
  logsEnabled: false
  infrastructureMonitoringEventsEnabled: true
splunkPlatform:
  endpoint: "https://scb-shw-328b2c1dccc6c5.stg.splunkcloud.com:8088/services/collector" # change this
  # Required for Splunk Enterprise/Cloud (if `endpoint` is specified). Splunk
  # HTTP Event Collector token.
  token: "713f8450-a165-48db-9d7c-bffdec931140"

  # Name of the Splunk event type index targeted. Required when ingesting logs to Splunk Platform.
  index: "main" #change this
  logsEnabled: true #change this
  # If you enable metrics collection, make sure that `metricsIndex` is provided as well.
  metricsEnabled: false
cloudProvider: "aws" #change this
distribution: "eks" #change this
logsEngine: "fluentd" 
